import { formatSpeciesLabel } from "@saasclinics/lib";

/** @deprecated use formatSpeciesLabel from @saasclinics/lib — kept for existing imports */
export const formatSpeciesDisplay = formatSpeciesLabel;
