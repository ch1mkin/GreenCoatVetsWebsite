import { FullPagePawLoading } from "@/components/web/full-page-paw-loading";

/** Shown while the root `/` page segment loads. */
export default function Loading() {
  return <FullPagePawLoading />;
}
